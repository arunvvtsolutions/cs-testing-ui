import { Box } from '@mui/material';
import React from 'react';
const ConnectToMentor = async () => {
  // const data = await getPaymentProducts();

  return <Box className="dashBoard_Container">{/* <PaymentDetails paymentDetailsData={data} /> */}</Box>;
};

export default ConnectToMentor;
