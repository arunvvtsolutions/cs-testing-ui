import { Typography } from '@mui/material';
import React from 'react';

const ErrorComponent = () => {
  return <Typography>{'Something went wrong please refresh the page'}</Typography>;
};

export default ErrorComponent;
